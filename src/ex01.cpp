# include <iostream>

using namespace std;

int main(){
    int x,y;
    cout << "Enter X: ";
    cin >> x;
    cout << "\nEnter Y: ";
    cin >> y;
    cout << "\n X + Y = " << (x + y) << endl;
    cout << "\n X - Y = " << (x - y) << endl;
    cout << "\n X * Y = " << (x * y) << endl;
    cout << "\n X / Y = " << (x / y) << endl;

  return 0;
}
